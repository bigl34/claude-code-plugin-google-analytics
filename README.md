# claude-code-plugin-google-analytics

Claude Code plugin for google-analytics-manager.

## Installation

1. Clone this repository
2. Copy `config.template.json` to `config.json` and configure
3. Install dependencies: `cd scripts && npm install`

## License

MIT
